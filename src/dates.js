// import { parse, format } from "date-fns";

// function processTime(timeString) {
//   const parsedTime = parse(timeString, 'HH:mm', new Date());
//   // Do something with the parsedTime
//   console.log(parsedTime);
// }

// // Example usage
// processTime('15:30');